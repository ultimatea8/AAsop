** important **
** important **

in func.js  > OptionSelector and other areas, add an "Alert" & "Warning" option.
Make the alert option red, and make warnining yellow when displayed.

** important **
** important **

allow calltaker to transfer answers to a different incdent type. 
like a 17 -> 18 and keep previous answers

auto generate after final question asked

allow calltakers to save all incident tabs that are open.
consolidate tabs by 10, and place incident type and time started.


time stamp every answer? for supervisor logs?



-Starting Script-
Agency Name, Operator "last name" 
To help you as quick as possible we have some questions we need you to answer
"What is the address of the emergency?"

Agency Name, Non-emergency
"How can I help you"


-Ending script-
"Call us back if anything changes, or if you have more information."


-- 
Children 
A. Neglected, abandoned or unattended: 
1. Questions to ask: 
a. Address where child/children are located?
b. Age of child/children? 
c. Description if applicable? 
d. Location of parents if known? 
2. Enter a call for dispatch (unit will respond to investigate the validity of call).
B. Child locked inside vehicle: 
1. Obtain address and notify Miami Fire Department.
2. Enter a call for dispatch. 
a. Obtain description of vehicle and exact location. 
b. Is the vehicle engine running? 
--



---------------- OLD CODE --------------
/* 
    $.each($sopLv2, function(indexa, vala) {
    if ( (typeof vala)=="object" ) {
        $sopLv2B = SOPdb[sopLv0][0][sopLv1][0][indexa];
            outPUT += `<div id="${fCount}" class="line row"><div class="input-group mb-3 tex col-2"><select class="form-select" id="iGroup${fCount}">`;
            outPUT+= OptionSelector("T");
            outPUT+=`</select> </div>
                <div class="form-floating mb-3 col-10"> <input type="text" class="form-control" id="misc${fCount}" placeholder="Yes/No">
                    <label for="misc${fCount}">${indexa}</label>
                </div> 
                </div>`;
            $("#fields").append(outPUT);
            outPUT=""; fCount++;
        $.each($sopLv2B, function(indexb, valb) {
            if ( (typeof valb)=="object" ) {
                $sopLv2C = SOPdb[sopLv0][0][sopLv1][0][indexb][0][indexc];
                $.each($sopLv2C, function(indexc, valc) {
                    console.log("This is going to sopLv2C");
                });
            }else{ 
                outPUT += `<div id="${fCount}" class="line row">
                    <div class="input-group mb-3 ms-4 tex col-2">
                        <select class="form-select" id="iGroup${fCount}">`;
                outPUT+= OptionSelector(indexb);
                outPUT+=`
                        </select>
                    </div>
                    <div class="form-floating mb-3 col">
                        <input type="text" class="form-control" id="misc${fCount}" placeholder="Yes/No">
                        <label for="misc${fCount}">${valb}</label>
                    </div> 
                </div>`;
                $("#fields").append(outPUT);

                outPUT=""; fCount++;
                // console.log(indexb+':', valb);
            }
        });
    }else { 
        console.log(vala);
    }
    }); 
/* */